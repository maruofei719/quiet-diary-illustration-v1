interface:
  display_name: "Quiet Diary Illustration"
  short_description: "Create stable reference-locked photo posters"
  default_prompt: "Use $quiet-diary-illustration-v1 in Reference-Locked Standard Mode to analyse these photographs, select exactly one compatible layout recipe, compile the fixed four-paragraph prompt, generate the poster, inspect it against the quality gate, and return the image, recipe, and exact prompt."
