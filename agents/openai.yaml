interface:
  display_name: "Quiet Diary Illustration"
  short_description: "Deconstruct photos into original quiet posters"
  default_prompt: "Use $quiet-diary-illustration-v1 in Deconstructed Standard Mode. Treat these photographs as a material library, select one Recipe A–D, execute extract, remove, rescale, and recompose, generate the poster, inspect it against the reverse-reconstruction quality gate, and return the image, recipe, and exact prompt."
